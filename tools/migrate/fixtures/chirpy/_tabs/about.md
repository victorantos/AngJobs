---
# Chirpy tab pages often carry no title — the layout supplies it.
icon: fas fa-info-circle
order: 4
---

About this test blog. Chirpy tabs resolve to `/:title/`, so `/about/` doesn't
change and needs no redirect — but the page must still be imported.
